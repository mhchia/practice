# -*- coding: utf-8 -*-

# This line is just like the list operation *10, which will extend the string by duplicating it 10 times.
print 'ha' * 10

# 1. The comma in the end of line makes print doesn't change to a new line.
# 2. In python, one line should not exceed 80 characters.
print 'a' + 'b' + 'c' + 'd',
print 'e' + 'f' + 'g' + 'h'
